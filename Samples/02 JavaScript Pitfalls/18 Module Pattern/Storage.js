var MyApp = MyApp || {};

MyApp.Storage = (function () {

    var lastActivity;

    function load() {
    }

    function save() {
    }

    return {
        load: load,
        save: save,
    };

})();
